/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package gradle.project.app;

import gradle.project.list.LinkedList;

import static gradle.project.utilities.StringUtils.join;
import static gradle.project.utilities.StringUtils.split;
import static gradle.project.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
